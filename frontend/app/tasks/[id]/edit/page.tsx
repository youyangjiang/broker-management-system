import { EntityEditForm } from "../../../components/EntityEditForm";
import { Shell } from "../../../components/Shell";
export default async function EditTaskPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; return <Shell title="编辑任务 / Editar tarefa"><EntityEditForm endpoint={`/tasks/${id}`} redirectTo={`/tasks/${id}`} fields={[{ name: "title", label: "任务标题 / Título da tarefa", required: true }, { name: "assigned_to_user_id", label: "负责人 / Responsável", type: "select", optionsEndpoint: "/users", optionLabelKey: "full_name", required: true }, { name: "priority", label: "优先级 / Prioridade", type: "select", options: [{ value: "low", label: "低 / Baixa" }, { value: "medium", label: "中 / Média" }, { value: "high", label: "高 / Alta" }], required: true }, { name: "status", label: "状态 / Status", type: "select", options: [{ value: "open", label: "待处理 / Pendente" }, { value: "in_progress", label: "处理中 / Em andamento" }, { value: "done", label: "已完成 / Concluída" }], required: true }, { name: "due_date", label: "截止时间 / Prazo", type: "datetime-local" }, { name: "reminder_at", label: "提醒时间 / Lembrete", type: "datetime-local" }, { name: "description", label: "说明 / Descrição", type: "textarea", full: true }]} /></Shell>; }
